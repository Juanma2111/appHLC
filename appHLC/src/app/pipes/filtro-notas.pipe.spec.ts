import { FiltroNotasPipe } from './filtro-notas.pipe';

describe('FiltroNotasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroNotasPipe();
    expect(pipe).toBeTruthy();
  });
});
